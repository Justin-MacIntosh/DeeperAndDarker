import { IconDiamond } from "@tabler/icons-react";

const GemIcon = ({ size }: { size: number }) => {
  return <IconDiamond viewBox="0 2 24 24" className="inline" size={size}/>;
}
export default GemIcon;
